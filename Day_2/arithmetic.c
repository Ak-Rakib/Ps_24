#include <stdio.h>
int main()
{
    int a = 4, b = 5;
    // int sum = a+b;
    // int sub = b-a;
    // int div = a/b;
    // float div = a*1.0/b;
    // printf("%f", div);
    int mod = b % a;
    printf("%d", mod);
    return 0;
}