#include<stdio.h>
int main()
{
    // if i want,then below the stirng showing three steps. using loop, initialize {} value and other step showing below
    char a[6] = "Rakib";
    printf("%s", a);
    return 0;
}